- 解决方案统一通过 `FufuLauncher.sln` 管理 C# 与 C++ 双语言项目，所有平台配置集中在 GlobalSection 中声明。
- C# 主程序采用 MVVM 分层（Models / Services / ViewModels / Views / Themes），各职责按目录隔离。
- C++ 原生功能以 DLL 形式封装并通过显式头文件导出，C# 侧通过 P/Invoke 调用。